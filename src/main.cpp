#include <vector.h>
// #include <matrix.h>

using std::cout;
using std::endl;

int main()
{
    Vector<int>* a = new Vector<int>(1);
    
    a->emplace_back(1);
    // int b = a.pop();
    // a(0) = 1;

    // cout << a << endl;


    return 0;
}